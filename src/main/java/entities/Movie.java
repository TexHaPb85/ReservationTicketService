package entities;

/**
 * name, number of room,
 * type of movie(original, translated),
 * status of movie(pre-premiere, premiere,  available),
 * duration of movie show( from, to),
 * price of ticket, category
 */
public class Movie {
    private String name;
    private String numberOfRoom;
    //type of movie(original, translated)
    //status of movie(pre-premiere, premiere,  available)
    //duration of movie show( from, to)
}
